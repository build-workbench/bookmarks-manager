# 2026-02-13 变更记录

## 已完成

- **工程化：.gitignore 更新与构建产物清理**
  - `.gitignore` 新增 IDE（`.vscode`/`.idea`）、OS（`Thumbs.db`/`desktop.ini`）、构建产物（`*.tsbuildinfo`/`vite.config.js`/`vite.config.d.ts`）、coverage、日志等忽略规则。
  - 取消 git 跟踪 `tsconfig.node.tsbuildinfo` 和 `vite.config.js`（之前已被跟踪的构建产物）。

- **工程化：项目配置增强**
  - 版本升级至 `1.0.0`，补充 `description`/`author`/`license`/`engines` 元信息。
  - 新增 `clean`、`validate` npm scripts；`build` 脚本增加 `tsc -b` 类型检查。
  - `vite.config.ts`：新增 `@/` 路径别名、PWA icons、Workbox 运行时缓存、构建分包（manualChunks）。
  - ESLint：升级 `es2022`，新增 `consistent-type-imports`/`no-console`/`eqeqeq` 等规则。
  - Prettier：补充 `arrowParens`/`endOfLine`/`bracketSpacing` 等配置。
  - `.editorconfig`：新增 YAML/JSON/Makefile 专属规则。
  - `.prettierignore`：同步忽略 `dev-dist`/`coverage`/`*.tsbuildinfo` 等。

- **代码质量：路径别名迁移**
  - 全部 `src/` 下的相对路径 import 迁移为 `@/` 别名形式，提升可读性与重构安全性。
