# 2025-12-18 变更记录

## 已完成

- **文档体系补齐（/docs）**
  - 新增 `docs/README.md` 作为文档入口。
  - 新增 `docs/PRD.md`：完成 Roadmap 范围裁剪（P0/P1/P2/归档）与验收标准。
  - 新增 `docs/DESIGN.md`：补齐分层架构、数据流、模块边界与后续设计要点。

- **对外文档对齐（README/FEATURES/QUICKSTART）**
  - README：新增 docs 入口；移除 Cytoscape 描述；更新 AI 描述与 Roadmap（P0/P1/P2）；修正 `lint` 脚本说明。
  - FEATURES：同步拖拽导入/搜索高亮的实现状态；更新 AI 路线为“本地报告模板 + 可复制 Prompt → 适配器式 BYOK”。
  - QUICKSTART：补充 docs 入口；同步拖拽与 AI 描述；更新备份说明（P1 备份与恢复后置）。

- **P0：导入体验补齐（上传合并）**
  - 支持拖拽导入书签 HTML 文件（含拖拽态视觉反馈）。
  - 新增“已导入文件”列表（按来源文件聚合统计）。
  - 支持移除单个来源文件（从待合并原始数据中剔除，并提示重新合并）。
  - 增加更细的导入/合并阶段加载状态提示，并在处理期间禁用相关交互避免重复触发。

- **P0：搜索体验升级（搜索页面）**
  - 搜索结果命中词高亮（标题 / URL / 路径）。

- **P0：高级过滤 + 导出增强（搜索页面）**
  - 新增高级过滤：按域名 / 一级目录 / 目录关键字 / 时间范围过滤（可与搜索组合）。
  - 新增导出选项：支持导出“当前筛选结果 / 全量”。
  - 新增导出选项：支持“保留目录结构 / 平铺导出（不保留目录）”。
  - 结果列表支持分页加载（默认 50 条，逐步加载更多）。
