# Deferred change

`batch-edit-tags-system` was deferred during the repository hardening pass on 2026-04-23.

Reason:

- the change had grown into a 117-task expansion that no longer matched the repository's closure-oriented goal
- keeping it active made `openspec list` misleading

The original proposal, design, tasks, and spec deltas are preserved in this archive folder for future re-scoping if the feature set is revisited.
